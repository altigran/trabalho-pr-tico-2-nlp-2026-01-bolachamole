# --- Avaliação ---
deepeval==4.0.6
pytest==9.0.3

# --- Treinamento / PEFT ---
transformers==5.12.0
trl==1.6.0
peft==0.19.1
accelerate==1.14.0
datasets==5.0.0
bitsandbytes==0.49.2      # necessário para QLoRA (4-bit)

# --- Núcleo / utilidades ---
torch==2.12.0              # use a build de CUDA compatível com seu ambiente (Colab)
huggingface_hub==1.16.1
sqlparse==0.5.5           # opcional: ajuda na extração/normalização do SQL gerado
pyyaml==6.0.3
